
// Question-1. What do you mean by arrays give one example of array and how you can declared an array?

// an array is a collection of elements of the same data type stored in contiguous (continuous) memory locations.
#include<stdio.h>
int main(){
int numbers[5] = {10, 20, 30, 40, 50};
printf("%d\n", numbers[0]);  // output 10
printf("%d\n", numbers[3]);  // outpur 40
return 0;
}

//Question.2  What do you mean by functions? Write the types of functions and give one example in code of each type.

//A function is a block of code that performs a specific task.
//Instead of writing the same code repeatedly, we can put it in a function and reuse it whenever needed.
//Type of function 
//1.liberery function(predefined).it is type of function which is stored by devloper it can not be changed.
// example.printf,scanf etc.
//2.user defind function- A user-defined function is a function created by the programmer to perform a specific task.
//example.
#include <stdio.h>

// User-defined function
int add(int x, int y) {
    return x + y;
}

int main() {
    // Predefined function example
    printf("Hello mam!\n");

    // User-defined function example
    int result = add(7, 3);  // Function call
    printf("Sum = %d\n", result);

    return 0;
}
   
// Question-3. What do you mean by Pointers? Write a code how you can initialise the pointer.
    
// A pointer is a variable that stores the memory address of another variable.
    #include<stdio.h>
    int main()
    int a = 10;
    int *ptr;   // pointer declaration
    ptr = &a;   // pointer initialization

    printf("Value of a: %d\n", a);
    printf("Address of a: %p\n", &a);
    printf("Value using pointer: %d\n", *ptr);

    return 0;
}
Question 4
#include<stdio.h>
int main()
    // Question-4. What are the types of array? Write in a code 1D & 2D included.
    // Types of Array: 
    // 1. One Dimensional (1D)
    // 2. Two Dimensional (2D)

    // 1D Array
    int arr[5] = {1, 2, 3, 4, 5};
    printf("1D Array Elements:\n");
    for(int i=0; i<5; i++)
        printf("%d ", arr[i]);

    printf("\n\n");

    // 2D Array
    int mat[2][3] = { {1, 2, 3}, {4, 5, 6} };
    printf("2D Array Elements:\n");
    for(int i=0; i<2; i++) {
        for(int j=0; j<3; j++)
            printf("%d ", mat[i][j]);
        printf("\n");
    }

    return 0;
}
QUESTION 5


#include<stdio.h>
int main()
{
    // Question-5. Explain the types of Operators in code.
    // Types: Arithmetic, Relational, Logical, Assignment, Increment/Decrement

    int a = 10, b = 5;

    // 1. Arithmetic Operators
    printf("Arithmetic: %d %d %d %d %d\n", a+b, a-b, a*b, a/b, a%b);

    // 2. Relational Operators
    printf("Relational: %d %d\n", a>b, a==b);

    // 3. Logical Operators
    printf("Logical: %d %d\n", (a>b && b>0), (a<b || b>0));

    // 4. Assignment Operator
    a += 5;
    printf("Assignment: %d\n", a);

    // 5. Increment/Decrement Operators
    a++; b--;
    printf("Inc/Dec: %d %d\n", a, b);

    return 0;
}
